service: () -> {
    doOperation: (text, text, text) -> (record {result:nat32; operationCreator:principal; operation:text});
    getOperations: () -> (vec record {result:nat32; operationCreator:principal; operation:text}) query;
}
